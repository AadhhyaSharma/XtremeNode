{
  "name": "xtremenode-stream-server",
  "version": "1.0.0",
  "description": "XtremeNode independent WebRTC stream server on port 8000 with API key auth",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "add-key": "node server.js --add-key",
    "list-keys": "node server.js --list-keys",
    "remove-key": "node server.js --remove-key"
  },
  "dependencies": {},
  "engines": { "node": ">=16" },
  "license": "MIT"
}
